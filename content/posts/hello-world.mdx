---
title: "Hello, World"
date: "2026-03-08"
tags: ["personal"]
excerpt: "The first post. A placeholder to make sure everything is wired up correctly."
description: "The first post on Ahmed Zafar's blog — a placeholder to make sure everything is wired up correctly."
published: true
ogImage: ""
---

This is the first post. Everything you're reading is rendered from a `.mdx` file sitting in `content/posts/`.

## How it works

Each post is a Markdown file with a frontmatter block at the top. The frontmatter controls the title, date, tags, and SEO metadata. The rest is just Markdown.

## Writing

You can use all standard Markdown here — **bold**, *italic*, [links](https://ahmedzafar.me), inline `code`, and code blocks:

```python
def greet(name: str) -> str:
    return f"Hello, {name}"
```

> Blockquotes look like this.

Lists work too:

- Item one
- Item two
- Item three

Delete this post whenever you're ready to write a real one.
